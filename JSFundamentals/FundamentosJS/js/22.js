// Selectores del DOM

// De cero a máximo un elemento
const heading = document.querySelector ('.heading')
console.log(heading)
console.log(heading.textContent)
console.log(heading.tagName)
console.log(heading.classList)
console.log(heading.id)

const enlaces = document.querySelectorAll ('.navegacion a') // retorna todas las coincidencias
console.log(enlaces)